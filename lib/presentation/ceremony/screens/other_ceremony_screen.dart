import 'package:flutter/material.dart';

class OtherCeremonyScreen extends StatelessWidget {
  const OtherCeremonyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          "OTHER CEREMONY",
        ),
      ),
    );
  }
}
